export enum UserRol {
  STANDARD = "standard",
  BOSS = "boss",
  PREMIUM = "premium",
}

export enum NotificationType {
  MESSAGE = "message",
  WARNING = "warning",
  ERROR = "error",
  INFO = "info",
}

